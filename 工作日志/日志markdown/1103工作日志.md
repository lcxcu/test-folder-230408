1. 匹配时增加了三个条件：对公司类型（LTD，CO等）、国家地区、缩写字符串是否被名称包含：
如下:
```
#如果公司类型不同，就比较公司类型
        
            match = [matchcp[i].append(0)  for i in range(0,3)]
            matchcp_new = [matchcp[i] for i in range(0,3)]
            for cp_sy_n in matchcp_new:
                if uk_abbr[3] == cptype[cleaned_name.index(cp_sy_n[0])]:
                                            cp_sy_n[2] += 1
                                            matchcp_new = sorted(matchcp_new, 
                                                             key=lambda
                                                             matchcp:matchcp[2]
                                                             ,reverse =True)       
```
```

比较是否包含有缩写字段,如果有的话，把位置往前放
            uk_abbr[2].split(' ')
            for ub in uk_abbr[2]:
                if ub in matchcp:
                        for i,j in zip(range(0,5,2),range(3)):
                            if cptype[cleaned_name.index(matchcp[j][0])] == uk_abbr[3]:
                                matchcp[j],matchcp[0] = matchcp[0],matchcp[j]
                    
            for i,j in zip(range(0,5,2),range(3)):
                Matchcpname.write(indexnow+1, i+2, 
                                  full_name[cleaned_name.index(matchcp[j][0])])
                Matchcpname.write(indexnow+1, i+3,
                                  matchcp[j][1])
```
